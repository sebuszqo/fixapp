version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fixapp
      POSTGRES_PASSWORD: fixapp
      POSTGRES_DB: fixapp
    ports: ["5432:5432"]
    volumes: [dbdata:/var/lib/postgresql/data]
volumes:
  dbdata: {}
