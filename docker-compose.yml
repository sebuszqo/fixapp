services:
  api:
    build: .
    ports: ["8080:8080"]
    environment:
      PORT: "8080"
      DB_HOST: db
      DB_PORT: "5432"
      DB_USER: fixapp
      DB_PASSWORD: fixapp
      DB_NAME: fixapp
      DB_SSLMODE: disable
      JWT_SECRET: dev-secret-change-in-production
      LOG_LEVEL: debug
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:5173,http://localhost:3000,http://localhost:5174,http://localhost:3001,http://127.0.0.1:5173,http://127.0.0.1:3000}
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fixapp
      POSTGRES_PASSWORD: fixapp
      POSTGRES_DB: fixapp
    ports: ["5432:5432"]
    volumes: [dbdata:/var/lib/postgresql/data]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fixapp"]
      interval: 2s
      timeout: 5s
      retries: 5

volumes:
  dbdata: {}
